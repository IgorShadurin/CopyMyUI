# Seed code overrides

Production gallery examples use the compact Swift fallback generated by
`prisma/seed.ts`. Add a `<component-slug>.swift` file here only when an example
needs a hand-maintained override.

The former generated corpus is intentionally excluded from Git and retained in
the ignored local recovery archive documented in `.local-backups/README.txt`.
